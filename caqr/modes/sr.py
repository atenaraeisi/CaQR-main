def run_sr_caqr(input_argument, device=None, verbose=0):
    raise NotImplementedError(
        "SR-CaQR is not implemented yet. It will use its own device-aware "
        "compilation flow rather than the QS pair-selection loop."
    )
